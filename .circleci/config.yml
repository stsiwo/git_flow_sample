version: 2
jobs:
  build:
    branches:
      only:
        - master
    docker: 
      - image: alpine:3.7
    steps:
      - run:
          name: Run Spa
          command: |
            echo 'Running Spa Testing'
